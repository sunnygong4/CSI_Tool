# Desktop / CLI core
#
# The core CR3 parser and native extraction flow still use the Python standard
# library. The dependencies below are for the Ubuntu web service, API server,
# storage integration, and web/integration tests.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
jinja2>=3.1,<4.0
boto3>=1.35,<2.0
httpx>=0.28,<1.0
